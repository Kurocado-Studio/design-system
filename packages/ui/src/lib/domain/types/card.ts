export interface CardVariantToken {
  bg: string;
  fg: string;
  radius: string;
  padding: string;
  paddingBody: string;
  paddingHeader: string;
  paddingFooter: string;
}

export interface CardProps {
  className?: string;
}
